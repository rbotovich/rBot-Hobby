%nav.navbar.navbar-inverse{:role => "navigation"}
  .container
    .container-fluid
      %ul
        %a.navbar-brand{:href => root_path} rBot
      %ul
        .navbar-right
          %a
            - if user_signed_in?
              = link_to "Sign Out", destroy_user_session_path, :method => :delete
            - else
              = link_to "Log in",  new_user_session_path

